import { SectionBody } from '@/components/admin-shell/section-layout'
import { RingGroupsPanel } from '@/components/shifts/ring-groups-panel'

export function RingGroupsSection() {
  return (
    <SectionBody>
      <RingGroupsPanel />
    </SectionBody>
  )
}
