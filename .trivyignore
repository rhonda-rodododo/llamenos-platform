# False positive: RCS channel config UI form field placeholder text
# matches gcp-service-account pattern but is not a real credential
gcp-service-account

# False positive: bun-types documentation cache contains example
# Stripe key patterns from documentation, not real secrets
stripe-secret-token
