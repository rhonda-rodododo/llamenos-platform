# False positive: GCP service account detected in minified JS bundle.
# Source: src/client/components/admin-settings/rcs-channel-section.tsx
# The "gcp-service-account" pattern matches a UI form placeholder string:
#   placeholder='{"type": "service_account", ...}'
# This is a user-facing input field hint, not a real credential.
gcp-service-account

# False positive: Stripe secret token example in bun-types documentation.
# Source: /build/.bun/install/cache/bun-types@*/docs/runtime/secrets.mdx
# The pattern matches example/tutorial content showing what a Stripe secret
# token looks like, not an actual secret.
stripe-secret-token
