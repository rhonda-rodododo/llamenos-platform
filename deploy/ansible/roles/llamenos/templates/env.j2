# Llamenos Environment Variables — managed by Ansible
# Do not edit manually; changes will be overwritten on next deploy.
{#
  All worker-container env vars are defined ONCE, in
  deploy/ansible/templates/env/_worker-required-env.j2, and included here via
  lookup() so this role (the monolithic/demo deploy path) and
  roles/llamenos-app (the per-service production path) can never drift out
  of sync again. This is the root-cause fix for #716/#771: this file used to
  hand-duplicate a subset of roles/llamenos-app/templates/env/app.j2 and was
  missing DATABASE_URL entirely, so the app container failed to boot on the
  demo/staging deploy path. See that shared file's header comment, and
  deploy/ansible/scripts/check-required-env.py, which fails CI if this
  render ever stops containing a var apps/worker/lib/config.ts requires.
#}
{{ lookup('ansible.builtin.template', playbook_dir ~ '/../templates/env/_worker-required-env.j2') }}
DOMAIN={{ domain }}
ACME_EMAIL={{ acme_email }}
