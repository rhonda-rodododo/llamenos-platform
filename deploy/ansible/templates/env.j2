# Llamenos Environment Variables — managed by Ansible
# Do not edit manually; changes will be overwritten on next deploy.
#
# Generated: {{ ansible_date_time.iso8601 | default('unknown') }}

# ─── Application ─────────────────────────────────────────────
ADMIN_PUBKEY={{ admin_pubkey }}
HOTLINE_NAME={{ hotline_name }}
ENVIRONMENT={{ environment }}
HMAC_SECRET={{ hmac_secret }}

# ─── PostgreSQL ──────────────────────────────────────────────
PG_PASSWORD={{ pg_password }}
PG_POOL_SIZE={{ pg_pool_size }}

# ─── Storage (RustFS) ───────────────────────────────────────
STORAGE_ACCESS_KEY={{ storage_access_key }}
STORAGE_SECRET_KEY={{ storage_secret_key }}
STORAGE_BUCKET={{ storage_bucket }}
STORAGE_ENDPOINT={{ storage_endpoint }}

# ─── Telephony ───────────────────────────────────────────────
TWILIO_ACCOUNT_SID={{ twilio_account_sid }}
TWILIO_AUTH_TOKEN={{ twilio_auth_token }}
TWILIO_PHONE_NUMBER={{ twilio_phone_number }}

# ─── Transcription ───────────────────────────────────────────
WHISPER_URL=http://whisper:8080/v1/audio/transcriptions
WHISPER_MODEL={{ whisper_model }}
WHISPER_DEVICE={{ whisper_device }}

# ─── Nostr Relay (Core) ─────────────────────────────────────
SERVER_NOSTR_SECRET={{ server_nostr_secret }}
NOSTR_RELAY_URL={{ nostr_relay_url | default('ws://strfry:7777') }}

{% if 'monitoring' in compose_profiles %}
# ─── GlitchTip (Error Tracking) ───────────────────────────
GLITCHTIP_SECRET_KEY={{ glitchtip_secret_key }}
GLITCHTIP_DOMAIN={{ glitchtip_domain | default('https://' + domain + '/glitchtip') }}
GLITCHTIP_DSN={{ glitchtip_dsn }}
GLITCHTIP_EMAIL_URL={{ glitchtip_email_url | default('consolemail://') }}
GLITCHTIP_FROM_EMAIL={{ glitchtip_from_email | default('noreply@' + domain) }}
{% endif %}

# ─── Desktop Distribution Domains ────────────────────────────
DOWNLOADS_DOMAIN={{ downloads_domain | default('downloads.' + domain) }}
UPDATES_DOMAIN={{ updates_domain | default('updates.' + domain) }}

{% if 'asterisk' in compose_profiles %}
# ─── Asterisk ────────────────────────────────────────────────
ARI_USERNAME={{ ari_username }}
ARI_PASSWORD={{ ari_password }}
BRIDGE_SECRET={{ bridge_secret }}
{% endif %}
