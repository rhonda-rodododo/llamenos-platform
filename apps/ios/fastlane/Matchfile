git_url(ENV["MATCH_GIT_URL"]) # e.g. git@github.com:your-org/llamenos-certificates.git

type("appstore") # Default; overridden per lane when needed

app_identifier("org.llamenos.hotline")
team_id("KN6NH935ZU")

# Git branch to store certs/profiles (one branch per type)
git_branch("main")

# Clones repo to a temp dir — no local storage of certs
shallow_clone(true)
clone_branch_directly(true)
