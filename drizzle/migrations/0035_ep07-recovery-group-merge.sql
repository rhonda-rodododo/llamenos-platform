-- This migration originally re-created hub_recovery_group_shares, hub_recovery_groups,
-- recovery_session_contributions, recovery_sessions, and user_recovery_envelopes (plus
-- their constraints/indexes) as an EP07 branch-merge dump. Every one of those objects
-- was already created by migrations 0030_clammy_rachel_grey and 0031_cloudy_magus, which
-- run earlier in this journal, making the entire body a duplicate that fails from-scratch
-- schema setup (relation "hub_recovery_group_shares" already exists). Left as a no-op to
-- preserve the applied-migration hash history.
